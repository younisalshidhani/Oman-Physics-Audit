import streamlit as st
import fitz 
import google.generativeai as genai

# إعدادات الصفحة والواجهة
st.set_page_config(page_title="المقوم الذكي - سلطنة عمان", layout="wide")
st.title("🛡️ نظام التدقيق الآلي للاختبارات (فيزياء 12)")

with st.sidebar:
    st.header("الإعدادات")
    api_key = st.text_input("أدخل مفتاح API الخاص بك:", type="password")

if api_key:
    try:
        genai.configure(api_key=api_key)
        # استخدام الموديل المستقر المتاح في منطقتك
        model = genai.GenerativeModel('gemini-1.5-flash') 
        
        uploaded_file = st.file_uploader("ارفع ملف الاختبار (PDF)", type="pdf")
        
        if uploaded_file:
            with st.spinner("جاري تحليل المفردات وصياغة الجدول..."):
                doc = fitz.open(stream=uploaded_file.read(), filetype="pdf")
                text = "".join([page.get_text() for page in doc])
                
                # توجيهات الخبير لتحويل البيانات إلى جدول
                prompt = f"""
                بصفتك خبير جودة اختبارات في سلطنة عمان، حلل الاختبار المرفق وصمم تقريراً مختصراً جداً.
                يجب أن يكون المخرج الأساسي عبارة عن جدول Markdown بالأعمدة التالية:
                (رقم المفردة | الدرجة | نوع هدف التقويم | نوع الملاحظة | الملاحظات | التعديل المقترح)
                
                المعايير التقنية للتحليل:
                1. المجموع الكلي: 10 درجات.
                2. المستويات: (معرفة، تطبيق، استدلال).
                3. العلميات: مطابقة درس "تأثير دوبلر" ص 32.
                4. الفنيات: وجود 4 بدائل للمتعدد، جودة الصياغة.
                
                بعد الجدول، اذكر فقط:
                - التوصية النهائية (في سطر واحد).
                - نسبة مطابقة الاختبار للمعايير (%).
                
                نص الاختبار:
                {text}
                """
                
                response = model.generate_content(prompt)
                st.success("✅ اكتمل التقرير الفني!")
                st.markdown("### 📊 نتائج التدقيق والمطابقة:")
                st.markdown(response.text)
                
    except Exception as e:
        st.error(f"حدث خطأ في الاتصال: {e}")
else:
    st.info("يرجى إدخال مفتاح API في القائمة الجانبية للبدء.")
