import streamlit as st
import fitz 
import google.generativeai as genai

st.set_page_config(page_title="المقوم الذكي - سلطنة عمان", layout="wide")
st.title("🛡️ نظام التدقيق الآلي للاختبارات (فيزياء 12)")

with st.sidebar:
    st.header("الإعدادات")
    api_key = st.text_input("أدخل مفتاح API الخاص بك:", type="password")

if api_key:
    try:
        genai.configure(api_key=api_key)
        # استخدام الموديل المتاح في حسابك
        model = genai.GenerativeModel('gemini-1.5-flash') 
        
        uploaded_file = st.file_uploader("ارفع ملف الاختبار (PDF)", type="pdf")
        
        if uploaded_file:
            with st.spinner("جاري استخراج البيانات وتحليلها محترافياً..."):
                doc = fitz.open(stream=uploaded_file.read(), filetype="pdf")
                text = "".join([page.get_text() for page in doc])
                
                # توجيهات التقرير الجدولية الصارمة
                prompt = f"""
                بصفتك خبير جودة اختبارات في سلطنة عمان، حلل الاختبار التالي بدقة.
                يجب أن يكون الرد في شكل جدول Markdown بالأعمدة التالية:
                (رقم المفردة، الدرجة، نوع هدف التقويم، نوع الملاحظة، الملاحظات، التعديل المقترح).
                
                المعايير المطلوبة: مجموع الدرجات 10، تغطية "تأثير دوبلر" ص 32، 4 بدائل للمتعدد.
                
                بعد الجدول، اذكر ما يلي:
                1. التوصية النهائية المختصرة.
                2. نسبة مطابقة الاختبار للمعايير (%).
                
                نص الاختبار:
                {text}
                """
                
                response = model.generate_content(prompt)
                st.success("✅ اكتمل التدقيق بنجاح!")
                st.markdown("### 📋 تقرير التدقيق الفني المعتمد:")
                st.markdown(response.text)
                
    except Exception as e:
        st.error(f"حدث خطأ: {e}")
else:
    st.info("يرجى إدخال مفتاح API في القائمة الجانبية للبدء.")
